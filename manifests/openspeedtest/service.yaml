apiVersion: v1
kind: Service
metadata:
  name: openspeedtest
  namespace: openspeedtest
spec:
  type: ClusterIP
  selector:
    app: openspeedtest
  ports:
    - name: http
      port: 3000
      targetPort: 3000
