apiVersion: v1
kind: Service
metadata:
  name: ovms
  namespace: ovms
spec:
  selector:
    app: ovms
  ports:
    - name: api
      port: 8000
      targetPort: api
    - name: stt-gateway
      port: 8080
      targetPort: stt-gateway
